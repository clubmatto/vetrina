---
layout: page.liquid
title: DB Diff
permalink: /products/db-diff/
description: Know when your data drifts. Compare rows across databases using hash-based checksums — blazing fast when tables match, pinpointing every difference when they don't.
---
<div class="coming-soon-ribbon-container">
  <span class="coming-soon-badge">Coming Soon</span>
</div>
<div class="showcase-header">
  <div class="showcase-page-icon">{{ 'database' | lucide }}</div>
  <h1 class="showcase-page-title">DB Diff</h1>
  <p class="showcase-page-tagline">Know when your data drifts</p>
</div>

<div class="coming-soon-features-grid">
  <div class="coming-soon-feature">
    <h3>Multi-Engine</h3>
    <p>PostgreSQL, MySQL, and ClickHouse — mix freely</p>
  </div>
  <div class="coming-soon-feature">
    <h3>Blazing Fast</h3>
    <p>Server-side MD5 chunking, only hashes cross the wire</p>
  </div>
  <div class="coming-soon-feature">
    <h3>Pinpoint Precision</h3>
    <p>Automatic drill-down to the exact differing rows</p>
  </div>
  <div class="coming-soon-feature">
    <h3>Column Control</h3>
    <p>Include or exclude columns to skip noise</p>
  </div>
</div>

<div class="coming-soon-waitlist" x-data="{ submitted: false }">
  <p>Leave your email and we'll let you know when DB Diff is ready.</p>
  <form
    x-show="!submitted"
    @submit.prevent="
      fetch($el.action, {
        method: 'POST',
        body: new FormData($el),
        headers: { 'Accept': 'application/json' }
      }).then(r => { if(r.ok) submitted = true })
    "
    action="https://formspree.io/f/YOUR_FORM_ID"
    method="POST"
  >
    <input type="email" name="email" required placeholder="your@email.com">
    <button type="submit" class="btn btn-primary">Join Waitlist</button>
  </form>
  <div class="waiting-list-submitted" x-show="submitted">
    <p>Thanks! We'll be in touch.</p>
  </div>
</div>
