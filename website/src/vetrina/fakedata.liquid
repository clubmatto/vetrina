---
layout: page.liquid
title: FakeData
permalink: /vetrina/fakedata/
go_import: true
description: Open source CLI tool for generating fake/test data. 80+ generators, multiple output formats, streaming, and templates.
image: /assets/img/fakedata/basic-light.gif
image_width: 1300
image_height: 650
---
<a
  href="https://github.com/clubmatto/vetrina/blob/main/fakedata/README.md"
  class="github-ribbon"
  aria-label="View on GitHub"
>
  {{ 'github' | lucide }}
</a>

<div class="showcase-header">
  <div class="showcase-page-icon">{{ 'layers' | lucide }}</div>
  <h1 class="showcase-page-title">FakeData</h1>
  <p class="showcase-page-tagline">Zero-config test data for your stack</p>
</div>

<div class="terminal-window">
  <div class="terminal-header">
    <span class="terminal-dot red"></span>
    <span class="terminal-dot green"></span>
    <span class="terminal-title">demo</span>
  </div>
  <div class="terminal-body terminal-gif">
    <img src="/assets/img/fakedata/basic-light.gif" alt="FakeData basic usage demo" loading="lazy" class="theme-light">
    <img src="/assets/img/fakedata/basic-dark.gif" alt="FakeData basic usage demo" loading="lazy" class="theme-dark">
  </div>
</div>

<h2 class="fakedata-section-title">Use Cases</h2>

<div class="fakedata-tabs" x-data="{ tab: 'testing' }">
  <div class="fakedata-tab-nav" role="tablist">
    <button role="tab" :class="{ active: tab === 'testing' }" @click="tab = 'testing'" :aria-selected="tab === 'testing'">Testing</button>
    <button role="tab" :class="{ active: tab === 'development' }" @click="tab = 'development'" :aria-selected="tab === 'development'">Development</button>
    <button role="tab" :class="{ active: tab === 'load-testing' }" @click="tab = 'load-testing'" :aria-selected="tab === 'load-testing'">Load Testing</button>
    <button role="tab" :class="{ active: tab === 'templates' }" @click="tab = 'templates'" :aria-selected="tab === 'templates'">Templates</button>
  </div>

  <div class="fakedata-tab-panels">
    <div class="fakedata-tab-content">
      <div class="fakedata-tab-body" x-show="tab === 'testing'">
        <div class="fakedata-tab-body-text">
          <p>
            Deterministic test data with seeds and CSV output. Reproduce the same results every time for reliable test
            suites.
          </p>
          <div class="terminal-command">
            <span class="terminal-prompt">$</span> fakedata --seed 42 -n 10 -H email country
          </div>
        </div>
      </div>

      <div class="fakedata-tab-body" x-show="tab === 'development'">
        <div class="fakedata-tab-body-text">
          <p>
            Quick mock data for APIs with NDJSON. Choose from 80+ built-in generators including names, emails, URLs, and
            even dinosaurs.
          </p>
          <div class="terminal-command">
            <span class="terminal-prompt">$</span> fakedata --format ndjson -n 3 name email company
          </div>
        </div>
      </div>

      <div class="fakedata-tab-body" x-show="tab === 'load-testing'">
        <div class="fakedata-tab-body-text">
          <p>
            Stream thousands of rows to test database performance and API limits. Pipe to a file, count lines, and stop
            when ready.
          </p>
          <div class="terminal-command">
            <span class="terminal-prompt">$</span> fakedata --stream email country | head -5
          </div>
        </div>
      </div>

      <div class="fakedata-tab-body" x-show="tab === 'templates'">
        <div class="fakedata-tab-body-text">
          <p>Custom Go templates for any output format. Define your own schema and generate JSON, YAML, or any text.</p>
          <div class="terminal-command"><span class="terminal-prompt">$</span> fakedata -T user.tmpl -n 3</div>
        </div>
      </div>
    </div>

    <div class="fakedata-tab-demos">
      <div class="fakedata-tab-demo" :class="{ visible: tab === 'testing' }">
        <div class="terminal-window">
          <div class="terminal-header">
            <span class="terminal-dot red"></span>
            <span class="terminal-dot yellow"></span>
            <span class="terminal-dot green"></span>
            <span class="terminal-title">Testing</span>
          </div>
          <div class="terminal-body terminal-gif">
            <img
              src="/assets/img/fakedata/use-case-testing-light.gif"
              alt="FakeData testing demo"
              loading="lazy"
              class="theme-light"
            >
            <img
              src="/assets/img/fakedata/use-case-testing-dark.gif"
              alt="FakeData testing demo"
              loading="lazy"
              class="theme-dark"
            >
          </div>
        </div>
      </div>

      <div class="fakedata-tab-demo" :class="{ visible: tab === 'development' }">
        <div class="terminal-window">
          <div class="terminal-header">
            <span class="terminal-dot red"></span>
            <span class="terminal-dot yellow"></span>
            <span class="terminal-dot green"></span>
            <span class="terminal-title">Development</span>
          </div>
          <div class="terminal-body terminal-gif">
            <img
              src="/assets/img/fakedata/use-case-development-light.gif"
              alt="FakeData development demo"
              loading="lazy"
              class="theme-light"
            >
            <img
              src="/assets/img/fakedata/use-case-development-dark.gif"
              alt="FakeData development demo"
              loading="lazy"
              class="theme-dark"
            >
          </div>
        </div>
      </div>

      <div class="fakedata-tab-demo" :class="{ visible: tab === 'load-testing' }">
        <div class="terminal-window">
          <div class="terminal-header">
            <span class="terminal-dot red"></span>
            <span class="terminal-dot yellow"></span>
            <span class="terminal-dot green"></span>
            <span class="terminal-title">Load Testing</span>
          </div>
          <div class="terminal-body terminal-gif">
            <img
              src="/assets/img/fakedata/use-case-load-testing-light.gif"
              alt="FakeData load testing demo"
              loading="lazy"
              class="theme-light"
            >
            <img
              src="/assets/img/fakedata/use-case-load-testing-dark.gif"
              alt="FakeData load testing demo"
              loading="lazy"
              class="theme-dark"
            >
          </div>
        </div>
      </div>

      <div class="fakedata-tab-demo" :class="{ visible: tab === 'templates' }">
        <div class="terminal-window">
          <div class="terminal-header">
            <span class="terminal-dot red"></span>
            <span class="terminal-dot yellow"></span>
            <span class="terminal-dot green"></span>
            <span class="terminal-title">Templates</span>
          </div>
          <div class="terminal-body terminal-gif">
            <img
              src="/assets/img/fakedata/templates-light.gif"
              alt="FakeData templates demo"
              loading="lazy"
              class="theme-light"
            >
            <img
              src="/assets/img/fakedata/templates-dark.gif"
              alt="FakeData templates demo"
              loading="lazy"
              class="theme-dark"
            >
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<h2 class="fakedata-section-title">Get Started</h2>

<div class="showcase-grid">
  <div class="showcase-grid-content">
    <p class="fakedata-intro-text">FakeData is built for developers who need realistic test data fast.</p>

    <div class="fakedata-features-grid">
      <div class="fakedata-feature">
        <h3>80+ generators</h3>
        <p>People, geography, web, time, types, and fun categories</p>
      </div>
      <div class="fakedata-feature">
        <h3>Multiple formats</h3>
        <p>Tab-separated, CSV, or NDJSON output</p>
      </div>
      <div class="fakedata-feature">
        <h3>Streaming mode</h3>
        <p>Generate data indefinitely for load testing</p>
      </div>
      <div class="fakedata-feature">
        <h3>Templates</h3>
        <p>Custom output with Go templates (JSON, YAML, any format)</p>
      </div>
      <div class="fakedata-feature">
        <h3>Deterministic</h3>
        <p>Use seeds for reproducible results every time</p>
      </div>
      <div class="fakedata-feature">
        <h3>Shell completion</h3>
        <p>Bash, zsh, and fish support</p>
      </div>
    </div>
  </div>
  <div class="fakedata-grid-terminal">
    <div class="terminal-window">
      <div class="terminal-header">
        <span class="terminal-dot red"></span>
        <span class="terminal-dot yellow"></span>
        <span class="terminal-dot green"></span>
        <span class="terminal-title">install</span>
      </div>
      <div class="terminal-body">
        <div class="terminal-comment"># Install via Go or Homebrew</div>
        <div class="terminal-line">
          <span class="terminal-prompt">$</span> go install matto.club/vetrina/fakedata@latest
        </div>
        <div class="terminal-line"><span class="terminal-prompt">$</span> brew install clubmatto/tap/fakedata</div>
      </div>
    </div>
  </div>
</div>

<h2 class="fakedata-section-title" id="generators">What Can You Generate?</h2>

<div class="terminal-window fakedata-browse" x-data="browser">
  <div class="terminal-header">
    <span class="terminal-dot red"></span>
    <span class="terminal-dot yellow"></span>
    <span class="terminal-dot green"></span>
    <span class="terminal-title">search for generators here {{ 'arrow-down' | lucide: size: 14 }}</span>
  </div>
  <div class="terminal-body">
    <div class="terminal-line fakedata-browse-line">
      <span class="terminal-prompt">$</span> fakedata
      <span class="fakedata-browse-cursor" x-show="!query && !focused">█</span>
      <input type="text" x-model="query" @focus="lastSelected = null; focused = true" @blur="focused = false" class="fakedata-browse-input" autocomplete="off">
      <button x-show="query" @click="clear" class="fakedata-browse-clear" aria-label="Clear search">&times;</button>
    </div>
    <div class="fakedata-browse-results" x-show="query.trim() && !lastSelected && matches.length">
      <template x-for="item in matches" :key="item[0]">
        <div class="fakedata-browse-result" @click="select(item[0], item[1])">
          <span class="fakedata-browse-result-name" x-html="highlight(item[0])"></span>
          <span class="fakedata-browse-result-desc" x-html="highlight(item[1].desc)"></span>
        </div>
      </template>
    </div>
    <div class="fakedata-browse-none" x-show="query.trim() && !lastSelected && !matches.length">
      <p class="fakedata-browse-none-text">No generators match "<span x-text="query"></span>". Try one of these:</p>
      <div class="fakedata-browse-suggestions">
        <template x-for="s in suggestions" :key="s">
          <span class="fakedata-browse-suggestion" @click="select(s, generators[s])" x-text="s"></span>
        </template>
      </div>
    </div>
    <div class="fakedata-browse-output" x-show="output.length && lastSelected">
      <template x-for="(line, i) in output" :key="i">
        <div class="fakedata-browse-output-line" x-text="line"></div>
      </template>
    </div>
  </div>
</div>

<script id="generators-data" type="application/json">{{ generators | json }}</script>

<h2 class="fakedata-section-title">FakeData Pro</h2>

<div class="showcase-more-section">
  <p class="fakedata-more-text">
    Need to generate data directly in your database? <strong>FakeData Pro</strong>
    connects to your database and generates realistic data automatically — no export/import needed.
  </p>

  <div class="showcase-ctas">
    <a href="/products/fakedata/" class="btn btn-primary">Learn More</a>
  </div>
</div>

<script>
  document.querySelectorAll('.terminal-command').forEach((el) => {
    el.addEventListener('click', async () => {
      const text = [...el.childNodes]
        .filter((n) => n.nodeType === 3)
        .map((n) => n.textContent)
        .join('')
        .trim();
      try {
        await navigator.clipboard.writeText(text);
        el.classList.add('copied');
        setTimeout(() => el.classList.remove('copied'), 1500);
      } catch {}
    });
  });
</script>
